@model Models.ViewModels.VentaMostrarVM


<div class="container mt-5">
	<div class="card shadow-sm">
		<div class="card-header py-3 bg-second-primary">
			<h6 class="m-0 font-weight-bold text-white">Selecciona un Método de Pago</h6>
		</div>
		<form method="POST" asp-action="ProcesarPago" id="paymentForm">
			<div class="card-body">
				<div class="mb-3">
					<div class="col-sm-6">
						<label asp-for="VENTAS.TipoPago" class="form-label">Tipo de Pago</label>
						<select class="form-control" asp-for="VENTAS.TipoPago" required>
							<option value="" selected disabled>Selecciona método</option>
							<option value="Tarjeta de Crédito">Tarjeta de Crédito</option>
							<option value="Sinpe">Sinpe</option>
							<option value="Dinero Disponible">Dinero Disponible</option>
						</select>
					</div>
				</div>

				<!-- En esta parte hay que añadir la lógica para que el cliente confirme sus datos de pago -->

				@* <div id="creditCardInfo" class="mb-3" style="display:none;">
                    <label class="form-label">Nombre del Titular</label>
                    <input asp-for="VENTAS.NombreTitular" type="text" class="form-control mb-2" placeholder="Titular de la tarjeta" />

                    <label class="form-label">Número de Tarjeta</label>
                    <input asp-for="VENTAS.NumeroTarjeta" type="text" class="form-control mb-2" placeholder="Número de tarjeta" />

                    <div class="row">
                        <div class="col-md-6">
                            <label class="form-label">CVV</label>
                            <input asp-for="VENTAS.CVV" type="text" class="form-control" placeholder="CVV" />
                        </div>
                        <div class="col-md-6">
                            <label class="form-label">Fecha de Expiración</label>
                            <input asp-for="VENTAS.FechaExpiracion" type="month" class="form-control" />
                        </div>
                    </div>
                </div> *@

				<!-- Es importante agregar la lógica para comprobar el dinero disponible y el Sinpe -->

				<div id="sinpeInfo" class="mb-3" style="display:none;">
					<button type="submit" class="btn btn-success">Confirmar Pago SINPE</button>
				</div>

				
			</div>	
			<div class="card-footer">
				<div class="row">
					<div class="col text-start">
						<button type="submit" class="btn btn-primary">Realizar Pago</button>
						<a asp-action="CarritoCompras" class="btn btn-success">Volver atras</a>
					</div>
				</div>
			</div>
		</form>
	</div>
</div>

@section Scripts {
	<!-- Referencias JS -->
	<script src="https://code.jquery.com/jquery-3.7.1.min.js"></script>
	<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
	<script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>

	<!-- JavaScript -->
	<script src="~/js/pagar.js"></script>
}
